#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here

	int T,I;
	cin>>T;
	while(T--){
	    cin>>I;
	    if(I <= 2) {
	        cout<<I<<endl;
	        continue;
	    }

        vector<int> arr(1000,0);
        int N = 0;
        arr[0]=1;
        N++;

        int carry = 0;
        for(int i = 2;i <= I; i++){

                for(int j = 0; j<N; j++) {
                    int Multi = arr[j] * i + carry;
                    arr[j] = Multi % 10;
                    carry = Multi / 10;

                }

                while(carry){
                    arr[N] = carry % 10;
                    carry = carry / 10;
                    N++;
                }

        }
        for(int i= N-1;i>=0;i--){
            cout<<arr[i];
        }
        cout<<endl;



	}
	return 0;
}
